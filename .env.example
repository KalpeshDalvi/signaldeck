# Workspace selected by the MVP server and UI.
SIGNALDECK_WORKSPACE_ID=billpay

# Local fallback key used by the ingestion APIs.
SIGNALDECK_INGESTION_KEY=dev-signaldeck-key

# Optional persistence. Leave blank to use the in-memory development store.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
